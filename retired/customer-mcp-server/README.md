# @loxtep/customer-mcp-server (retired)

This package **does not run**. Local stdio MCP is gone.

Use hosted MCP:

```json
{
  "mcpServers": {
    "loxtep": {
      "url": "https://mcp.loxtep.io/ai/mcp/stream"
    }
  }
}
```

Setup: [loxtep-plugins-skills](https://github.com/loxtepinc/loxtep-plugins-skills)

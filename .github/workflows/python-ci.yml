name: Python SDK CI

on:
  push:
    branches: [main]
    paths: ['python/**', 'shared/**']
  pull_request:
    branches: [main]
    paths: ['python/**', 'shared/**']

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ['3.10', '3.11', '3.12']
    defaults:
      run:
        working-directory: python
    steps:
      - uses: actions/checkout@v5

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -e ".[dev]"

      - name: Lint
        run: |
          # Pin major so CI doesn't inherit surprise default-rule expansions.
          # Rule set lives in python/pyproject.toml ([tool.ruff.lint]).
          pip install 'ruff>=0.8,<0.17'
          ruff check .

      - name: Test
        run: pytest
